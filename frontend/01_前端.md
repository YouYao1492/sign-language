# A. 前端 Checklist

## Login

* [X] frontend 檔案建制
* [ ] Login 頁面完成
* [ ] 可以呼叫 `POST /api/login`
* [ ] Token 可以保存
* [ ] 可以取得目前使用者角色
* [ ] `deaf / staff` 可以正確顯示

## Session

* [ ] 可以呼叫 `POST /api/sessions`
* [ ] 成功後取得 `session_id`
* [ ] 可以進入手語辨識界面

## Camera

* [ ] 可以請求 Camera 權限
* [ ] 可以顯示 Camera Preview
* [ ] 可以開始錄影
* [ ] 可以停止錄影
* [ ] 可以取得 Video Blob
* [ ] 可以處理 Camera 權限被拒絕

## Sign Recognition

* [ ] 可以將 Video Blob 放入 `multipart/form-data`
* [ ] 傳送 `video`
* [ ] 傳送 `session_id`
* [ ] 傳送 `scenario_id`
* [ ] 傳送 `language`
* [ ] 可以呼叫 `POST /api/recognize`
* [ ] 可以顯示 AI 回傳文字
