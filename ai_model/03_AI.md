# C. AI Checklist

## API

* [X] ai_model 檔案建制
* [ ] `POST /api/recognize`
* [ ] 接收 `multipart/form-data`
* [ ] 可以接收 Video
* [ ] 可以接收 `session_id`
* [ ] 可以接收 `scenario_id`
* [ ] 可以接收 `language`

## Recognition

* [ ] Video 可以送入 Recognition
* [ ] Recognition 可以產生中文文字
* [ ] Response 格式符合 Contract

## Mock

* [ ] Mock AI API 完成
* [ ] Mock Response 與正式 AI 相同
* [ ] Backend 可以直接切換 Mock / Real AI
